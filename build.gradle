plugins {
    id 'java'
    id 'war'
}

group = 'com.silvertown'
version = '1.0-SNAPSHOT'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

repositories {
    mavenCentral()
}

ext {
    junitVersion = '5.10.2'       // JUnit 5 테스팅 프레임워크
    springVersion = '5.3.39'       // Spring Framework 5.3.x 마지막 버전 (보안 패치 포함)
    lombokVersion = '1.18.38'      // Lombok 라이브러리 최신 버전 (2025년 기준)
    aspectjVersion = '1.9.20.1'    // AspectJ 최신 마이너 버전
    jacksonVersion = '2.12.7'      // Spring 5.3.x와 호환되는 Jackson 최신 안정 버전
    springSecurityVersion = '5.8.15' // Spring Security 버전
}

dependencies {
    // Spring Framework
    implementation "org.springframework:spring-context:${springVersion}"
    implementation "org.springframework:spring-webmvc:${springVersion}"
    implementation "org.springframework:spring-websocket:${springVersion}"
    implementation "org.springframework:spring-jdbc:${springVersion}"

    // Spring Security
    implementation "org.springframework.security:spring-security-web:${springSecurityVersion}"
    implementation "org.springframework.security:spring-security-config:${springSecurityVersion}"
    implementation "org.springframework.security:spring-security-oauth2-client:${springSecurityVersion}"

    // JWT
    implementation 'io.jsonwebtoken:jjwt-api:0.11.5'
    runtimeOnly 'io.jsonwebtoken:jjwt-impl:0.11.5'
    runtimeOnly 'io.jsonwebtoken:jjwt-jackson:0.11.5'

    // 스프링 데이터베이스 지원
    implementation "org.springframework:spring-tx:${springVersion}"

    // 데이터베이스 관련 의존성
    implementation 'com.mysql:mysql-connector-j:9.3.0'     // MySQL JDBC 드라이버
    implementation 'com.zaxxer:HikariCP:3.4.5'             // 커넥션 풀 라이브러리

    // Tomcat 9 / Servlet 4.0
    compileOnly 'javax.servlet:javax.servlet-api:4.0.1'

    // MyBatis
    implementation 'org.mybatis:mybatis:3.5.19'
    implementation 'org.mybatis:mybatis-spring:2.1.2'

    // MySQL
    runtimeOnly 'com.mysql:mysql-connector-j:9.3.0'

    // JSON
    implementation 'com.fasterxml.jackson.core:jackson-databind:2.19.1'
    implementation 'com.fasterxml.jackson.datatype:jackson-datatype-jsr310:2.19.1'

    // Redis Streams 알림 파이프라인
    implementation 'org.springframework.data:spring-data-redis:2.7.18'
    implementation 'io.lettuce:lettuce-core:6.2.6.RELEASE'

    // Firebase Cloud Messaging
    implementation 'com.google.firebase:firebase-admin:9.10.0'

    // Logging (SLF4J + Log4j2 console)
    implementation 'org.apache.logging.log4j:log4j-api:2.23.1'
    runtimeOnly 'org.apache.logging.log4j:log4j-core:2.23.1'
    runtimeOnly 'org.apache.logging.log4j:log4j-slf4j2-impl:2.23.1'
    runtimeOnly 'org.apache.logging.log4j:log4j-jcl:2.23.1'

    // Environment variables (.env)
    implementation 'io.github.cdimascio:dotenv-java:3.2.0'

    // API documentation (Spring MVC / non-Boot)
    implementation 'io.springfox:springfox-swagger2:3.0.0'
    implementation 'io.springfox:springfox-swagger-ui:3.0.0'

    // Validation (Spring Framework 5 / javax namespace)
    implementation 'javax.validation:validation-api:2.0.1.Final'
    implementation 'org.hibernate.validator:hibernate-validator:6.2.5.Final'
    implementation 'org.glassfish:javax.el:3.0.1-b12'

    // JSP view rendering
    implementation 'javax.servlet:jstl:1.2'

    // Lombok
    compileOnly 'org.projectlombok:lombok:1.18.38'
    annotationProcessor 'org.projectlombok:lombok:1.18.38'

    // Test
    testImplementation 'javax.servlet:javax.servlet-api:4.0.1'
    testImplementation 'org.junit.jupiter:junit-jupiter:5.10.5'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.10.5'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher:1.10.5'
    testImplementation 'com.h2database:h2:2.3.232'
    testImplementation "org.springframework:spring-test:${springVersion}"
    testImplementation 'org.hamcrest:hamcrest:2.2'
    testImplementation 'org.mockito:mockito-core:5.13.0'
    testImplementation 'com.fasterxml.jackson.dataformat:jackson-dataformat-xml:2.19.1'

    // Xerces XML 파서 - XML 내 한글 문자 처리 개선 (안정 버전 유지)
    implementation 'xerces:xercesImpl:2.12.2'

    // Azure Speech v2 WebSocket을 통한 송금 음성 인식
    implementation 'com.azure:azure-core:1.56.1'
    implementation group: 'com.microsoft.cognitiveservices.speech', name: 'client-sdk', version: '1.51.0', ext: 'jar'
}

tasks.named('test') {
    useJUnitPlatform()
    systemProperty 'runLocalAuthSmoke', System.getProperty('runLocalAuthSmoke', 'false')
    systemProperty 'runSpeechTokenSmoke', System.getProperty('runSpeechTokenSmoke', 'false')
    systemProperty 'speechSmokeBaseUrl', System.getProperty('speechSmokeBaseUrl', 'localhost:8080')
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}
