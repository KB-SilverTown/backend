package com.silvertown.domain.voice.enums;

/** Defines how long an adaptive guidance mode remains active in a voice session. */
public enum GuidanceScope {
    NEXT_RESPONSE,
    CURRENT_STEP,
    CURRENT_SESSION
}
